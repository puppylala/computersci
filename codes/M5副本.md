```python
#我的方法其实是不错的。但是由于我的反义序列长度一直在修改，这就导致我后来对于正义反义的索引计算不正确了。  
a=input()  
longest=""  
for i in range(len(a)):  
    for j in range(i+1,len(a)+1):  
        c=a[i:j]  
        if c==c[::-1]:  
            if len(c)>len(longest):  
                longest=c  
print(longest)
```